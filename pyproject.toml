[project]
name = "pyhql"
version = "0.0.10"
authors = [
    { name="Sylvain Jones", email="sylvainjones+hql@hashfastr.com" },
]
maintainers = [
    { name="Sylvain Jones", email="sylvainjones+hql@hashfastr.com" },
]
description = "Hash query language, a universal Query Language"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "License :: OSI Approved :: MIT License",
    #"Intended Audience :: DFIR",
]
license = "MIT"
license-files = ["LICENSE"]

dependencies = [
    "polars",
    "antlr4-python3-runtime==4.13.2",
    "elasticsearch",
    "splunk-sdk",
    "numpy",
    "requests",
    "ndjson",
    "oyaml",
    # "opensearch-py",
    "aiohttp",
    "boto3",
    "fastapi",
    "uvicorn",
    # "dfir_iris_client"
]

[project.urls]
Homepage = "https://github.com/Hashfastr/Hql"
Issues = "https://github.com/Hashfastr/Hql/issues"

[dependency-groups]
notebooks = ["marimo"]
dev = ["pytest"]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["Hql"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.marimo.runtime]
auto_reload = "modules"
watcher_on_save = "autorun" 
